apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "veraid-authority.fullname" . }}-test-services"
  labels:
    {{- include "veraid-authority.labels" (merge (dict "Component" "test") .) | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: pohttp
      image: busybox:1.32.0
      imagePullPolicy: IfNotPresent
      command: ['wget']
      args:
        - '--spider'
        - '-T'
        - '2'
        - http://{{ include "veraid-authority.fullname" . }}-pohttp:80
    - name: cogrpc
      image: busybox:1.32.0
      imagePullPolicy: IfNotPresent
      command: ['wget']
      args:
        - '--spider'
        - '-T'
        - '2'
        - http://{{ include "veraid-authority.fullname" . }}-cogrpc:8083
    - name: poweb
      image: busybox:1.32.0
      imagePullPolicy: IfNotPresent
      command: ['wget']
      args:
        - '--spider'
        - '-T'
        - '2'
        - http://{{ include "veraid-authority.fullname" . }}-poweb:8082
  restartPolicy: Never
